/**
 * useCart — re-exported from CartProvider for convenient import.
 *
 * Usage:
 *   import { useCart } from "@/components/cart/useCart"
 *   const { add, count, openCart } = useCart()
 */
export { useCart } from "./CartProvider";
